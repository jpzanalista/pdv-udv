# ---- Banco ----
# Em docker-compose (api dentro do Docker), host = "postgres:5432".
# Rodando na máquina (dev:host / db:migrate), use: postgresql://pdv:pdv@localhost:5440/pdv
DATABASE_URL=postgresql://pdv:pdv@postgres:5432/pdv

# ---- Nosso JWT (token exchange) ----
JWT_SECRET=troque-em-producao
JWT_REFRESH_SECRET=troque-em-producao-refresh
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=30d

# ---- Cognito (REUNI) — validação no backend ----
COGNITO_USER_POOL_ID=us-east-1_QCJtSIWeq
COGNITO_CLIENT_ID=4heu0nghf370dks1kt7lfob0m5
COGNITO_REGION=us-east-1

# ---- ASAAS (use a chave de SANDBOX no dev) ----
ASAAS_BASE_URL=https://api-sandbox.asaas.com/v3
ASAAS_API_KEY=

# ---- API / Web ----
API_PORT=3333
NEXT_PUBLIC_API_URL=http://localhost:3333
